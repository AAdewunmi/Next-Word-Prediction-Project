# --- Cross-platform line endings ---
* text=auto eol=lf

# --- Treat large/binary artifacts as binary (no diffs) ---
*.h5    binary
*.hdf5  binary
*.hdf   binary
*.ckpt  binary
*.tflite binary
*.joblib binary
*.pkl   binary
*.npz   binary
*.npy   binary

# --- Images & media ---
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.svg   text eol=lf

# --- Notebooks: keep as text to enable clean diffs in PRs ---
*.ipynb linguist-generated=false

# --- GitHub Linguist tuning (so Python dominates the language stats) ---
# Mark front-end/static assets and workflow files as "vendored" so they don't skew language %
# Adjust paths to match your repo layout.
# Core app code lives under nextword_site/** (Python), which we *do* want to count.

# Django templates and static assets
nextword_site/predictor/templates/** linguist-vendored
nextword_site/predictor/static/**    linguist-vendored

# Site-wide static (if present)
static/**                             linguist-vendored
templates/**                          linguist-vendored

# Sample data & model artifacts
sample_data/**                        linguist-vendored
models/**                              linguist-vendored
nextword_site/predictor/artifacts/**  linguist-vendored

# CI/config/docs
.github/**                            linguist-vendored
docs/**                               linguist-vendored

# Workspace/editor files
*.code-workspace                      linguist-vendored

# README and notebooks should still show up normally
README.md linguist-documentation
