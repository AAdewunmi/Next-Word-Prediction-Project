# --- OS junk ---
.DS_Store
Thumbs.db

# --- Python ---
__pycache__/
*.py[cod]
*$py.class

# --- Virtual envs (repo-root and common names) ---
/.venv/
.venv/
venv/
env/

# --- Packaging / build ---
build/
dist/
.eggs/
*.egg-info/
pip-wheel-metadata/

# --- Logs & temps ---
*.log
*.tmp

# --- Django ---
/staticfiles/
media/
db.sqlite3
local_settings.py

# --- Environment files ---
.env
.env.*
!/.env.example

# --- Tests / coverage ---
.pytest_cache/
.coverage
htmlcov/

# --- Jupyter ---
.ipynb_checkpoints/
*.ipynb

# --- IDE/editor ---
.vscode/
.idea/
*.iml

# --- ML artifacts (large binaries) ---
# Keep metadata under version control; ignore big model blobs by default.
predictor/artifacts/nextWordPredict/
*.h5
*.keras
*.ckpt
checkpoint
*.pb
# If you also want to avoid committing the tokenizer, uncomment:
# predictor/artifacts/tokenizer.pkl

# --- Node (if any frontend tooling later) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Requested explicit ignore ---
/path/
/.venv/
verv
.venv310
This `.gitignore` file covers common files and directories that should be ignored in a Python/Django project, including virtual environments, build artifacts, logs, and specific directories like `/path` and `/.venv`. Adjust as necessary for your specific project needs!